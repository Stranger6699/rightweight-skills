interface:
  display_name: "Review Changes"
  short_description: "Review changes for concrete defects and risks"
  default_prompt: "Use $review-changes to review these changes without modifying them."
policy:
  allow_implicit_invocation: true
