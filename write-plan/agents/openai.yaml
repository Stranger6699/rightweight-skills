interface:
  display_name: "Write Plan"
  short_description: "Plan complex implementation at useful granularity"
  default_prompt: "Use $write-plan to create a right-sized implementation plan for this change."
policy:
  allow_implicit_invocation: true
