interface:
  display_name: "Implement Change"
  short_description: "Implement changes with the smallest sufficient flow"
  default_prompt: "Use $implement-change to make this change with proportionate verification."
policy:
  allow_implicit_invocation: true
